// Placeholder so the app runs while the real storm module is being built —
// this file is about to be overwritten with hurricanes.
import * as THREE from 'three'

export function createStorms() {
  return { group: new THREE.Group(), update() {} }
}
